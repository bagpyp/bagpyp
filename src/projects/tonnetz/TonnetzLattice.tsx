/**
 * TonnetzLattice.tsx
 *
 * Next.js wrapper component for the Tonnetz Lattice application.
 * This component is loaded dynamically by ProjectDetail.tsx.
 *
 * Note: CSS is imported in _app.tsx due to Next.js global CSS restrictions.
 */

import App from './App';

const TonnetzLattice = () => {
  return <App />;
};

export default TonnetzLattice;
